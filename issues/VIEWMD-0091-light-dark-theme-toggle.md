---
id: VIEWMD-0091
title: Add a light/dark theme setting for admonition, table, and heading colors
status: in-progress
area: [render, config, cli]
effort: medium
created: 2026-08-19
updated: 2026-08-20
accepted_by: George Moses <gmo6nl@gmail.com>
accepted_at: 2026-08-20
commits: []
related: []
supersedes: []
changelog:
reason:
---

# Add a light/dark theme setting for admonition, table, and heading colors

## Summary

Add a `--theme {dark,light}` flag (and matching `theme` config-file key), defaulting to `dark` (today's existing, unlabeled palette), that selects between two built-in color palettes for the pieces of rendering that currently hardcode colors tuned for a dark terminal background — admonition callout borders/icons (`viewmd/render.py`'s `ViewmdBlockQuote`/`_AdmonitionKind`), the front-matter table, and any other fixed-color styling not already driven by per-diagram `classDef`/color logic.

## Motivation / problem

viewmd's admonition and table colors were evidently chosen against a dark background (unverified against a light one) with no way to override them; a user on a light terminal theme gets whatever contrast results, good or bad, with no escape hatch. Mermaid pie/quadrant charts already branch their palette on color-capability detection (`--color`/`NO_COLOR`/tty), establishing the precedent that viewmd's rendering can be palette-aware — this extends that same idea to a light/dark axis instead of only an on/off one.

## Requirements

1. MUST add `--theme {dark,light}` (default `dark`, i.e. today's existing colors, so this is purely additive — no behavior change for anyone who doesn't pass the flag).
2. MUST add a matching `theme` key to `viewmd/config.py`'s `Config`/`parse_config`, following the same `coalesce(CLI, config, default)` precedence as `--width`/`--color`.
3. MUST apply the `light` theme to at least: admonition callout colors (`_AdmonitionKind` definitions), the front-matter table's styling, and the table-of-contents' heading colors — the specific palette values are an implementation/design decision, not specified numerically here, but MUST be visually verified for adequate contrast on a light background (manual check, screenshot in the PR/issue discussion).
4. MUST apply `--theme` to the Mermaid quadrant chart's quadrant-background fills specifically (amended 2026-08-20, maintainer testing feedback: `--theme light` against `docs/mermaid-quadrant.md` showed dark quadrant backgrounds clashing with a light terminal) — the light theme's quadrant backgrounds MUST read as light, fitting a light terminal background, the same bar the non-Mermaid surfaces (requirement 3) already clear. MUST NOT otherwise change Mermaid diagram coloring beyond this — pie-chart fills and any other diagram-type coloring keep their existing color-capability-driven logic (truecolor/monochrome detection) untouched; those remain out of scope for this issue unless a further follow-up decides to unify the two systems.
5. MUST NOT attempt to auto-detect the terminal's actual background color (no OSC 11 query or similar) — explicit `--theme`/config only, since terminal background detection is unreliable across emulators and out of scope here.
6. MUST apply `--theme` to fenced-code-block syntax highlighting (amended 2026-08-20, maintainer testing feedback: `--theme light` still rendered code fences with a dark background/`monokai`-style highlighting, clashing with the light terminal alongside the now-correctly-light quadrant charts and admonitions) — `render_markdown`'s `ViewmdMarkdown(body, code_theme="monokai")` (`viewmd/render.py`) hardcodes a single Pygments theme regardless of `--theme`; MUST pick a light-appropriate Pygments theme for `theme == "light"` (e.g. a light monokai variant, or another light Pygments theme with adequate contrast) and keep `monokai` for `theme == "dark"` (today's existing, unchanged behavior).

## Non-goals

- No fully user-customizable/arbitrary color palette (no `--theme-file`) — two built-in named themes only.
- No auto-detection of light vs. dark terminal background.

## Design notes / links

Precedent: `viewmd/render.py`'s pie-chart rendering already branches its palette on color-capability (truecolor circle vs. monochrome bar) — this issue is the same kind of decision applied to a light/dark axis for the non-Mermaid rendering surfaces.

## Acceptance / verification

New tests in `tests/test_render.py` pinning `--theme light` output for an admonition block and the front-matter table against fixtures, alongside existing `--theme dark`(default)-equivalent fixtures unchanged. New test pinning `--theme light` quadrant-background output for `docs/mermaid-quadrant.md` (or an equivalent fixture) against `--theme dark`'s existing output. New test pinning `--theme light` fenced-code-block syntax-highlighting output against `--theme dark`'s existing (`monokai`) output for a fenced code block with a non-trivial lexer (e.g. Python or JSON) so the two themes' Pygments styles are visibly different. `./run-tests.sh` green.

## Peer review

- (agent, implementer self-review, not a substitute for an independent pass per AGENTS.md) `--theme {dark,light}` added to `build_parser()`, `Config`/`parse_config` (`theme` key, `THEME_CHOICES`), coalesced `coalesce(args.theme, cfg.theme, "dark")` exactly like `--width`/`--color`. Verified byte-for-byte that an omitted `--theme` (and `--theme dark` explicitly) reproduce pre-change output exactly (diffed against the prior commit's rendering for an admonition+front-matter+ToC doc; the only diff was the OSC8 hyperlink's random `id=`, unrelated to color). Verified CLI overrides config (`--theme dark` over a `theme = light` config file) and config applies when CLI omits the flag, both by diffing full end-to-end `python -m viewmd` output. Light palette sourced from GitHub Primer's own *light*-theme alert tokens (NOTE `#0969da`, TIP `#1a7f37`, IMPORTANT `#8250df`, WARNING `#9a6700`, CAUTION `#cf222e`) — the natural counterpart to the dark palette's already-documented Primer dark-theme source, not an arbitrary independent pick; front-matter table and ToC h2/h3 reuse the NOTE/IMPORTANT blues/purples for one consistent accent rather than inventing new colors. Confirmed `--theme` is plumbed through every render entry point that needed it (`render_markdown`, `render_front_matter_block`, `render_multi_file`, `display_document`/`display_multi_file`, `run`/`run_multi_file` and their `color_kwargs` closures used for link-follow/resize reloads) — caught by running the full suite, which failed three `fake_run` test doubles in `tests/test_main.py`/`tests/test_pager.py` for a missing `theme` kwarg until fixed, a real (if mechanical) plumbing gap the type checker wouldn't have caught since Python doesn't enforce it.
- Known scope narrowing, intentional per the issue: body Markdown headings (not just the ToC's own entries) keep their original color in both themes — only the ToC line's own style is retargeted, since requirement 3 named "the ToC's heading colors" specifically, not body headings; `test_toc_heading_color_differs_between_dark_and_light_theme` asserts this distinction explicitly (dark-magenta only inside `viewmd-toc:`-linked lines, not the later body heading lines reusing the same text). Directory-listing tables and a `.md` file opened by clicking through a directory listing (`run_directory_listing`'s own `open_path`) are not theme-aware, matching that entry point's pre-existing pattern of already omitting `full_front_matter`/`toc` too — not a new regression, but worth the maintainer's awareness if a light-themed directory browse ever becomes a real use case.
- Not independently verified: the light palette's actual on-screen contrast in a real light-background terminal (Primer's own tokens are documented ~4.5:1+ against white, but that is a source-material claim, not something checked against an actual terminal emulator here) — the issue's own acceptance criteria calls for a manual/screenshot check, which a non-interactive agent cannot perform; flagged to the maintainer as still outstanding before merge.
- (agent, independent review) No correctness bugs found. Traced `theme` through every call site by hand: `render_markdown`/`render_front_matter_block`/`render_multi_file`, `display_document`/`display_multi_file`, `run`/`run_multi_file`, and every `color_kwargs`/`loader` closure in `viewmd/interactive_pager.py` including `run()`'s own link-follow `open_path` (line 1351) and the width-toggle reload path — all correctly carry `theme`. Two closures do omit it, both pre-existing and harmless, not new regressions: `_front_matter_body_start` (line 216-221) calls `render_front_matter_block` without `theme`, but that call also passes `color=False`, so no palette is ever selected there anyway — it's only used to count rendered lines for the initial-scroll `body_start`; and `run_directory_listing`'s clicked-`.md`-file `open_path` (line 1413) passes `color_kwargs={}`, which was already the case on `develop` before this branch (confirmed via `git show develop:viewmd/interactive_pager.py`) and already omitted `full_front_matter`/`toc` the same way, so this is an existing gap this issue didn't introduce or worsen. `ViewmdBlockQuote.create()`: verified against rich's own source (`.venv/lib/python*/site-packages/rich/markdown.py`) that `element_class.create(self, token)` is always called with `self` being the top-level `Markdown`/`ViewmdMarkdown` instance whose own `__rich_console__` is executing, even for nested blockquotes — so the `viewmd_theme` stamp is read from the correct instance every time, and since `render_markdown` builds one fresh `ViewmdMarkdown` per call (no reuse/caching across renders) there's no cross-instance/cross-theme bleed risk in this single-threaded CLI. Requirement 4 (no Mermaid coloring change): grepped `viewmd/mermaid/` and `viewmd/preprocessors.py` for `theme` — zero hits relevant to this change (one unrelated pre-existing docstring word in the gantt renderer). Requirement 1 (byte-identical default output): did not just trust the self-review's claim — rendered the same multi-section sample doc (front matter, two admonition types, a table) through `develop`'s own `.venv` and this branch's `.venv` with `--color always` and no `--theme` flag, then again with `--theme dark` explicit, and diffed both against `develop`'s output myself; only difference in both cases was the OSC8 hyperlink's random `id=` number, exactly as claimed. Light-palette contrast: independently computed WCAG contrast ratios from the raw hex values against white (not just trusting the "Primer ~4.5:1+" claim) — NOTE 5.19:1, TIP 5.08:1, IMPORTANT 5.05:1, WARNING 4.87:1, CAUTION 5.36:1, table/ToC accent 5.19:1 — all clear AA (4.5:1) for normal text; none looked washed out or suspiciously borderline. Two real gaps found, neither a correctness bug: (1) **README.md is not updated** — `--width`/`--color`/`--full-front-matter`/`--toc` are all documented in the flag-examples section and the config-key table (README.md lines ~134-162), but `--theme`/`theme` appear nowhere in README, so a user reading the docs has no way to discover the flag exists (only `--help` mentions it). (2) **No end-to-end CLI/config coalesce test for `--theme` in `tests/test_main.py`** — `toc` and `full_front_matter` each have a full set of `main()`-level tests (`test_cli_toc_overrides_config_false`, `test_config_toc_false_omits_the_toc`, etc., using `capsys`) proving the CLI-over-config-over-default precedence actually works end to end; `theme` has no equivalent, only unit-level tests that pass `theme=` directly to `render_markdown`/`display_document` and a `parse_config` test. The self-review states this was checked manually by diffing CLI output, but nothing pins it as an automated regression test — notable given `AGENTS.md` itself documents this *exact* class of gap (VIEWMD-0043: "a comprehensive test suite passing is not proof a CLI flag actually works end to end... plumbing gap invisible to any test that supplies the correct value directly") as a standing lesson for this repo. Test suite: ran `./run-tests.sh` in this worktree — 1153 tests pass, ruff clean, pip-audit clean; the `issues --check` step fails, but only on two *unrelated* in-progress issues (VIEWMD-0089, VIEWMD-0090, each missing an `effort:` field) also present on `develop` itself before this branch — confirmed pre-existing and not caused by this change. Recommendation: correctness-wise this is ready to merge; I'd ask for the README doc gap to be fixed (small) and would like to see the missing `test_main.py` coalesce coverage added before calling it done, but neither blocks correctness. **This review does not substitute for the still-outstanding maintainer manual visual/contrast check on a real light-background terminal that the issue's own acceptance criteria (requirement 3) requires** — everything above is static/computed verification, not an actual look at the palette rendered in a terminal.
- (agent, quadrant theming per amended requirement 4) Implemented the amended requirement 4 (2026-08-20, maintainer manual-testing feedback that `--theme light` still left `docs/mermaid-quadrant.md`'s quadrant backgrounds dark): threaded a new `theme: str = "dark"` kwarg through the one call chain that reaches Mermaid rendering — `render_markdown` -> `preprocess` -> `render_mermaid_blocks` -> `viewmd.mermaid.render` -> (quadrant branch only) `quadrant.renderer.render` -> `_render_box` — following the exact same `if step is render_mermaid_blocks: ... theme=theme` conditional-pass-through pattern `preprocess` already used for `color`/`width`, so every other diagram type's `render()` branch (pie, gantt, gitGraph, xychart, sequence, flowchart, ER, packet, kanban, mindmap, block) receives no `theme` argument at all — confirmed by re-reading `viewmd/mermaid/__init__.py`'s `render()` after the edit, only the `_is_quadrant_diagram` branch's call changed. No other `preprocess(`/`render_markdown(` call site needed new plumbing: `render_markdown` was already the sole caller of `preprocess`, and every one of its own callers (`render_multi_file`, `pager.py`, `interactive_pager.py`'s several `render_markdown` calls including the link-follow/resize-reload closures) already threads `theme` into `render_markdown` from VIEWMD-0091's original admonition/table/ToC work, so this addition rides that same existing plumbing rather than needing a second pass through the pager. In `quadrant/renderer.py`: added `_lighten(hex_, factor)` mirroring `_darken`'s shape (blends each RGB channel toward 255 by `factor` instead of toward 0) and a `_BG_LIGHTEN_FACTOR = 0.82` constant; `_render_box`'s background-fill loop now picks `_lighten(..., _BG_LIGHTEN_FACTOR)` under `theme == "light"` and keeps `_darken(..., _BG_DARKEN_FACTOR)` otherwise, leaving `_QUADRANT_COLORS` (the foreground tints used for borders/points/text) completely untouched in both themes, per the amended requirement's explicit scope limit. Chose `0.82` by rendering `docs/mermaid-quadrant.md` with `--color always --theme light` at several candidate factors (0.5, 0.65, 0.82, 0.95) and comparing by eye against a white background: 0.5 still read as a mid-saturation tint, not "light"; 0.95+ was nearly indistinguishable from white and lost the four quadrants' visual distinctness from each other; 0.82 kept every fill unambiguously pale/pastel (each RGB channel at least 82% of the way to 255, e.g. quadrant 1's `#6f9fd8` fg becomes `#dceee4`-range bg) while the four quadrants stayed clearly distinguishable from one another and from plain white. Tests added: `tests/test_mermaid_quadrant.py` (6 new tests — light-vs-dark bg hex values differ and are lighter, light bg channels are all ≥200, foreground tints identical across themes, theme has no effect with `color=False`, omitted `theme` matches `"dark"`); `tests/test_render.py` (end-to-end `render_markdown(..., theme=...)` against a `docs/mermaid-quadrant.md`-equivalent quadrant doc, asserting dark/light bg codes differ and light is lighter, plus a `test_pie_chart_rendering_is_unaffected_by_theme` asserting byte-identical output across omitted/`"dark"`/`"light"` for a pie-chart doc); `tests/test_main.py` (`test_cli_theme_light_reaches_quadrant_background_fill_end_to_end`, invoking `main()` with `--theme light` vs `--theme dark` against a real file on disk and diffing the emitted `48;2;...` background codes — the VIEWMD-0043-class plumbing-gap check AGENTS.md calls for, not just a test that hands `theme=` to `render_markdown` directly). Verification: `./run-tests.sh` in this worktree — 1166 tests pass, ruff clean, pip-audit clean, completions up to date; `issues --check` still fails only on the same two pre-existing, unrelated in-progress issues (VIEWMD-0089, VIEWMD-0090, missing `effort:`) prior reviews already confirmed predate this branch. Manual CLI verification via this worktree's own `.venv`: `python -m viewmd --no-pager --color always --theme light docs/mermaid-quadrant.md` emits background fills `dceee4`/`e5edf7`/`f0e5f7`/`f7e9dc` (light pastels, all channels ≥220) vs. `--theme dark`'s (and omitted `--theme`'s) unchanged `112e1d`/`1f2c3c`/`311f3c`/`3c2611`-family shades (plus an unrelated shared `272822` code-block background present in both themes, from the syntax-highlighting theme, not quadrant coloring); every light-theme background's channel sum exceeds every dark-theme background's. Diffed the `--theme` -omitted and explicit-`--theme dark` renders of `docs/mermaid-quadrant.md` against `develop`'s own (pre-`--theme`) rendering of the same file, byte-for-byte after normalizing the OSC8 hyperlink's random `id=` — identical, confirming this addition is purely additive exactly like the rest of VIEWMD-0091. Confirmed the pie-chart MUST NOT clause: rendered `docs/mermaid-pie.md` with no `--theme` flag, `--theme dark`, and `--theme light` — the pie diagram's own rendered body (fenced-block content, excluding the document's unrelated ToC heading-color lines which requirement 3 already legitimately themes) is byte-for-byte identical across all three.
- (agent, gap fix following independent review) Addressed both non-blocking gaps the independent review flagged. README.md: added `--theme light` to the usage examples, a one-line prose description (admonition/front-matter-table/ToC-heading palette, default `dark`, no effect on Mermaid coloring), a `theme` row to the config-key table, a `theme = light` line in the sample config-file block, and `--theme`'s two literal choices to the shell-completion paragraph — following the exact placement/pattern already used for `--width`/`--color`/`--full-front-matter`/`--toc`. tests/test_main.py: added `test_cli_theme_overrides_config_light`, `test_config_theme_light_sets_it_when_cli_omits_theme`, `test_theme_defaults_to_dark_when_neither_cli_nor_config_set`, and `test_invalid_config_theme_exits_nonzero_without_traceback`, mirroring the structure of the existing `toc` coalesce tests (`test_cli_toc_overrides_config_false`/`test_config_toc_false_omits_the_toc`/`test_toc_is_on_by_default_for_a_multi_heading_document`/`test_invalid_config_toc_exits_nonzero_without_traceback`) — each runs `main()` end-to-end with `--color always` and asserts the exact truecolor SGR sequence (`#58a6ff` dark vs. `#0969da` light NOTE admonition color) present/absent in stdout, closing the plumbing-gap risk class AGENTS.md documents from VIEWMD-0043. `./run-tests.sh`: 1157 tests pass, ruff clean, pip-audit clean, completions up to date; `issues --check` still fails only on the same two pre-existing, unrelated in-progress issues (VIEWMD-0089, VIEWMD-0090, missing `effort:`) the independent review already confirmed predate this branch.
- (agent, code-fence theming per amended requirement 6) Implemented the amended requirement 6 (2026-08-20, maintainer manual-testing feedback that `--theme light` still rendered fenced code blocks with a dark `monokai`-style background/highlighting, clashing with the now-correctly-light quadrant charts and admonitions). Root cause confirmed by reading the code: `code_theme="monokai"` was hardcoded (regardless of `theme`) at two `ViewmdMarkdown(...)` construction sites — `render_markdown` (`viewmd/render.py`, line ~660) and interactive-pager `_load` (`viewmd/interactive_pager.py`, line ~263) — while a third site, `_markdown_with_tokens` (`viewmd/render.py`, line ~563, used for the title/ToC/rest token-split print path), already correctly *copied* `code_theme=source.code_theme` from the already-constructed `ViewmdMarkdown` rather than hardcoding it; confirmed by reading it, no change needed there once the two hardcoded sites pick the right theme. Fix: added `_CODE_THEME_BY_THEME = {"dark": "monokai", "light": "friendly"}` in `viewmd/render.py`, following the exact `_ADMONITIONS_BY_THEME`/`_TABLE_STYLE_BY_THEME` dict-lookup-with-dark-fallback pattern already used elsewhere in that file, and used it (via `_CODE_THEME_BY_THEME.get(theme, _CODE_THEME_BY_THEME["dark"])`) at both hardcoded call sites, reusing each site's already-threaded `theme` parameter (`render_markdown`'s own `theme` arg; `interactive_pager._load`'s `color_kwargs["theme"]`, the same dict `render_markdown` itself receives via `**color_kwargs`) — no new parameter needed anywhere. `_load`'s `ViewmdMarkdown` instance is only used for `heading_outline`, never printed, so its `code_theme` has no observable effect either way; fixed anyway for consistency, noted in a comment so a future reader doesn't wonder why it matters. Chose Pygments' built-in `"friendly"` theme for light after surveying this worktree's own `.venv/lib/python*/site-packages/pygments/styles/` rather than guessing a name that might not exist (confirmed `friendly` exists there, background `#f0f0f0`) — picked over `"default"`/`"xcode"`/`"vs"` (near-white, `#f8f8f8`/`#ffffff`/`#ffffff`) because `friendly`'s slightly off-white background and fuller per-token syntax coloring (not just black text on white) reads more clearly as a *themed* light surface, matching how the quadrant chart's own light theme (amended requirement 4) chose a pastel tint over the closest-to-white option rather than the starkest one. Inline code spans: confirmed via `rich/markdown.py` (`inline_code_theme: str | None = None`, then `self.inline_code_theme = inline_code_theme or code_theme` in `Markdown.__init__`) that rich itself already falls back inline-code's own Pygments theme to whatever `code_theme` the `Markdown`/`ViewmdMarkdown` instance was constructed with when no separate `inline_code_theme` is passed — viewmd never passes one — so inline code spans are automatically fixed by the same `code_theme` fix above; no separate hardcoded site exists for them, and `_markdown_with_tokens`'s existing `view.inline_code_theme = source.inline_code_theme` copy-through (unchanged) keeps that correct across the title/ToC/rest split too. `ViewmdCodeBlock.__rich_console__` (`viewmd/render.py`): confirmed it reads `self.theme`/`theme=self.theme` (rich's own `CodeBlock` machinery) off of whatever `Markdown` instance constructed it, with no `code_theme` string of its own — needed no change, only the constructor call needed the right string handed in. Tests added: `tests/test_render.py` (`test_code_fence_theme_light_differs_from_dark_and_is_not_the_old_dark_codes`, `test_code_fence_theme_defaults_to_dark_monokai_unchanged`, `test_code_fence_no_color_theme_output_is_unaffected_by_theme`, using a real ```python fence so Pygments actually emits per-token color codes to diff, not a bare code-less fence); `tests/test_main.py` (`test_cli_theme_light_reaches_code_fence_highlighting_end_to_end`, invoking `main()` end-to-end with `--theme light` vs `--theme dark` against a real file on disk and diffing emitted SGR codes minus the shared plain reset `\x1b[0m` — the VIEWMD-0043-class plumbing-gap check AGENTS.md calls for, matching the pattern the quadrant-theming review already established). Verification: `./run-tests.sh` in this worktree — 1170 tests pass, ruff clean, pip-audit clean, completions up to date; `issues --check` still fails only on the same two pre-existing, unrelated in-progress issues (VIEWMD-0089, VIEWMD-0090, missing `effort:`) prior reviews already confirmed predate this branch. Manual CLI verification via this worktree's own `.venv`: rendered `docs/example.md` (has a Python-fenced code block) with `--no-pager --color always --theme light` vs `--theme dark` vs `--theme` omitted — light theme's code fence emits `48;2;240;240;240` (the `friendly` theme's `#f0f0f0` background) with legible per-token colors (e.g. `38;2;0;112;32` green for `def`/keywords, `38;2;6;40;126` for function names, `38;2;64;112;160` for docstrings) instead of a dark box; dark theme's fence still emits `48;2;39;40;34` (monokai's own background), unchanged. Diffed `--theme` omitted vs. `--theme dark` explicit full-document output — identical except the OSC8 hyperlink's random `id=` numbers, the same pre-existing non-determinism every prior VIEWMD-0091 review already normalized past, confirming this addition stays purely additive for the dark/default path exactly like the rest of the issue.
