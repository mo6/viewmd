---
id: VIEWMD-0089
title: Show file size in directory listings and add an optional recursive depth
status: implemented
area: [render, cli]
effort: medium
created: 2026-08-19
updated: 2026-08-20
accepted_by: George Moses <gmo6nl@gmail.com>
accepted_at: 2026-08-20
commits: [c671c3b, c552652, c650ebe, 40e9f2a, 934244f, 7556c62]
related: []
supersedes: []
changelog: "[1.48.0]"
reason:
---

# Show file size in directory listings and add an optional recursive depth

## Summary

Two related, independently-shippable improvements to the bare-directory-listing view (`render_directory_listing` in `viewmd/render.py`, VIEWMD-0065): add a file-size column alongside the existing name/last-modified columns, and add an opt-in `--depth N` flag to descend into subdirectories rather than the current fixed one-level listing.

## Motivation / problem

Today's directory listing (used when a directory `path` argument has no `_Index.md`/`index.md`/`_index.md`) shows a file's title and last-modified time, one level deep, no recursion — useful as a landing page but limited as an actual browsing tool: there's no way to see how large a file is before opening it, or to get an overview of a small vault's structure without opening each subdirectory in turn.

## Requirements

1. MUST add a size column to the existing directory-listing table, formatted human-readably (e.g. `1.2K`, `340B`), for Markdown files; subdirectory rows show an entry count or stay blank (decide one, document it) rather than a byte size.
2. MUST add `--depth N` (default: `1`, today's existing behavior) that, when `N > 1`, lists entries from subdirectories up to `N` levels deep, each row indented to show its depth, still subdirectories-first-then-files, alphabetical within each directory as today.
3. MUST NOT change output for the default (`--depth` omitted or `1`) case beyond the added size column — existing depth-1 tests should only need updating for the new column, not restructuring.
4. SHOULD cap or warn on unreasonably large `--depth` values traversing very large trees, consistent with the tool's terminal-rendering purpose rather than becoming a general-purpose `find`.
5. MUST leave the "directory with an index file renders that file instead" behavior (VIEWMD-0065/0074) completely untouched — this issue only affects the bare-listing fallback path.

## Non-goals

- No `--depth` support for anything other than the bare directory-listing view (single documents and multi-file concatenation are unaffected).
- No sorting options beyond the existing subdirectories-first-then-alphabetical order.

## Design notes / links

Builds on `viewmd/render.py`'s existing `render_directory_listing`; `--depth` follows the same `coalesce(CLI, config, default)` pattern as `--width`/`--toc` in `viewmd/__main__.py` if it should also be config-settable (decide during implementation).

## Acceptance / verification

New/updated tests in `tests/test_render.py` covering: size column formatting for a few byte-size boundaries; `--depth 2`/`--depth 3` output against a fixture directory tree; `--depth 1`/omitted matches pre-existing fixtures plus the new column. `./run-tests.sh` green.

## Peer review

- (implementing agent, self-review only -- NOT the independent review AGENTS.md's Definition of Done requires before merge, since it's the same agent that wrote the diff; a genuinely independent pass is still needed): Read the full diff end to end against the five requirements and both non-goals before this line was written, then re-read it a second time deliberately looking for bugs rather than confirming intent. Found and fixed two real issues in that second pass: (1) `_entry_count()` (new, shows a subdirectory row's item count) called `os.listdir()` on every subdirectory to compute that count, including at the *default* `depth=1` -- meaning a permission-denied subdirectory would now crash the entire listing even though depth-1 never used to need read access to a subdirectory's own contents, violating requirement 3 ("MUST NOT change output for the default case beyond the added size column") in spirit by adding a new crash mode to the unchanged default path; fixed by catching `OSError` there and showing a blank size for that row instead, with a regression test (`test_render_directory_listing_unreadable_subdirectory_shows_blank_size_not_a_crash`). (2) `--depth`'s cap-warning was originally resolved unconditionally near the top of `main()`, before branching on whether the `path` argument is even a bare directory listing at all -- so `viewmd somefile.md --depth 999` printed the "`--depth 999` capped to 10" warning to stderr even though `--depth` has no effect there at all (violates the Non-goals: "single documents and multi-file concatenation are unaffected" -- a spurious warning is itself an effect); fixed by moving `_resolve_depth()`'s call into the directory-listing branch only, with a regression test asserting empty stderr for that case. Verified the CLI end-to-end by hand (not just tests, per AGENTS.md's VIEWMD-0043 lesson): built a real nested directory tree and ran `--depth` omitted/2/3/999 against it, confirming indentation, subdirectories-first ordering at each level, size-column formatting, and the capping warning all behave as intended. Two scoping decisions the issue left open, both documented in code comments: the size column sits between Title and Modified (not appended at the end); a subdirectory row's size column shows its own immediate-child entry count (`"N items"`/`"1 item"`), computed the same way at every depth level regardless of how deep `--depth` actually descends; only top-level (`level == 0`) rows stay click-navigable in the interactive pager -- a deeper row renders as plain (still-escaped) text rather than widening `_resolve_dir_target`'s documented "always an immediate child" contract, so navigating to a level-2+ entry requires clicking into its parent first (documented in README.md). `--depth` was made config-file-settable (`depth = N`, mirroring `width`/`toc`), capped at `MAX_DEPTH = 10` (chosen value, not specified by the issue) with a stderr warning rather than a hard error when exceeded, and deliberately given no effect on multi-file concatenation, matching the issue's own Non-goals text literally.
- (agent, independent review): Read the full diff (`git diff develop...feature/VIEWMD-0089`) cold, against all five requirements and both non-goals, without reusing the implementer's own write-up beyond its stated context. Checked the specific risk areas called out for this review directly rather than trusting either the tests or the self-review notes: (1) requirement 3's "byte-identical apart from the size column" claim isn't actually tested by fixture diffing (the depth-1 tests are loose substring checks, and `test_render_directory_listing_default_depth_matches_pre_existing_depth_1_output` only compares default-vs-explicit-`depth=1` against *itself*, never against pre-change output) -- so I verified it by hand: ran `render_directory_listing` against an identical fixture directory from both the `develop` and `feature/VIEWMD-0089` worktrees' own `.venv`s and diffed the two renders; the only difference was the inserted `Size` column, confirming the requirement genuinely holds, not just that the new test suite is internally consistent. (2) The click-navigation restriction ("only level-0 rows are click-navigable") is real, not just documented: confirmed in `render.py` that the `Text(...).stylize(Style(link=...))` branch is gated on `level == 0` for *both* dir and file rows, with every `level > 0` row falling through to a plain `escape()` string carrying no href at all -- so a depth>1 row has nothing for `_run`'s click handler to find, not a silent mis-resolve. Also confirmed `depth` correctly carries into a subdirectory opened by a click (closure captures the outer `depth` in `run_directory_listing`'s `make_loader`/`open_path`), and that this is exercised end-to-end (not just unit-level) by `test_run_directory_listing_depth_carries_over_into_a_navigated_subdirectory`. Found no correctness bugs and no requirement left unmet. Three minor, non-blocking findings: (a) all three shell completions (`completions/viewmd.bash`/`.fish`/`.zsh`) wire `--depth`'s argument to file-path completion (`compgen -f`, `-rF`, `:path:_files` respectively) -- the same completer `--config` uses -- even though `--depth` takes a positive integer, not a path; harmless but offers filenames as completions after typing `--depth `. (b) `_entry_count()` re-lists a subdirectory's contents via `_list_dir_entries()`/`os.listdir()` even when `_iter_listing_rows()` is about to (or already did) list that same directory to recurse into it at `depth > 1` -- every expanded subdirectory gets `os.listdir()`'d twice; not a correctness bug (bounded by `MAX_DEPTH`) but an avoidable redundant syscall worth revisiting if large-tree `--depth` performance ever matters. (c) test coverage gap: `test_depth_flag_caps_an_unreasonably_large_value` only exercises the cap+warning path via the CLI `--depth` flag, never via a config-file `depth` value exceeding `MAX_DEPTH` -- both funnel through the same `_resolve_depth()` so risk is low, but the combination itself is untested. Ran `./run-tests.sh`: 1164 tests pass, ruff and pip-audit clean; the only failing step is `issues --check`, and I confirmed (by running the same check against the primary `develop` worktree) that failure is pre-existing and unrelated to this diff -- two other in-flight issues (VIEWMD-0090, VIEWMD-0091) are missing an `effort:` field on `develop` itself. Considered ready to merge as-is; the three findings above are worth a follow-up but none block landing.
- (agent, fix following maintainer's manual testing feedback): The maintainer ran `viewmd issues/` interactively and found two real bugs the review above missed, from a screenshot of the actual terminal output. Bug 1: `run_directory_listing`'s `make_loader` (`interactive_pager.py`) rendered the table at the full configured width `w` before knowing whether the interactive pager would end up reserving `_SCROLLBAR_RESERVED_W` columns for a scrollbar (VIEWMD-0079) -- once a listing had enough rows to trigger one, `draw()`'s crop (`cw = term_w - reserved`) silently truncated the table's own right border and part of its rightmost column on every single row, showing as a `›` marker on every line and a missing right border, even though nothing actually needed horizontal scrolling. Fixed by rendering once, checking the resulting line count against `shutil.get_terminal_size().lines - 2` (matching `_run`'s own `body_h` computation), and re-rendering both colored/plain versions at `w - _SCROLLBAR_RESERVED_W` when it overflows, so the table already fits the space the scrollbar will actually leave. Investigated whether `run_multi_file`'s own embedded directory-listing (`directory_width`, fixed at the full terminal width, VIEWMD-0071) has the same edge: it does -- the fixed `directory_width` never shrinks for the scrollbar either -- so applied the identical narrowing there too, gated on there actually being a directory-listing entry (`text is None`) among `entries`, to avoid an unnecessary re-render for pure multi-markdown-file concatenations. Bug 2: `render_directory_listing`'s `Name` column (`viewmd/render.py`) had no `max_width`, and Rich's column-sizing algorithm treats a `no_wrap` column's minimum width as its full content width, so a long filename never shrank and squeezed every wrapping column (`Title`) into whatever was left -- the maintainer's screenshot showed a title wrapping across 6 narrow lines next to a 44-char filename. Fixed by adding `max_width=55, overflow="ellipsis"` to the `Name` column; chose 55 by checking actual filenames under `issues/` (median ~40 chars, all but the single longest of 27 top-level files fit within 55 without truncation) as a value that comfortably covers this project's own real names while still capping the worst case. Added regression tests: `tests/test_interactive_pager.py::test_run_directory_listing_loader_narrows_for_scrollbar_when_listing_overflows` and `::test_run_multi_file_loader_narrows_directory_width_for_scrollbar_when_overflowing` (both force a small terminal via `shutil.get_terminal_size`, capture the loader via a faked `_run`, and assert every rendered row's display width fits within `w - _SCROLLBAR_RESERVED_W`); `tests/test_render.py::test_render_directory_listing_caps_a_long_name_with_an_ellipsis` (an 83-char name truncates to the 55-char cap plus `…`) and `::test_render_directory_listing_title_column_wider_with_a_long_name_present` (reproduces the maintainer's own screenshot scenario -- a long filename plus that exact long title sentence -- and asserts the fixed table's `Title` column is wider than the same table built with the old uncapped `Name` column). Verified end-to-end in a real terminal, not just unit tests: used `tmux` (120x20) to run `./viewmd.sh issues/` against this project's own `issues/` directory -- confirmed the scrollbar (`█`/`░` glyphs) renders on the left with every row's table border intact and no `›` truncation marker anywhere, from top to bottom of the listing (`G` to jump to the end), and quit cleanly with `q`. Also directly compared `Title` column widths at a realistic width=120 against `issues/`'s real files: 21 chars before the fix vs. 23 after (modest here since the longest real name, 57 chars, sits close to the 55 cap already), and confirmed the effect is much more pronounced at wider terminals or with longer names (the 83-char synthetic case above). Ran `./run-tests.sh`: 1168 tests pass (4 new), ruff and pip-audit clean; `issues --check` still fails only on the same pre-existing, unrelated `effort:` gap on VIEWMD-0090/VIEWMD-0091 noted in the prior review.
- (agent, filename-truncation scheme per maintainer's second round of manual testing feedback): The maintainer tested the `max_width=55, overflow="ellipsis"` fix above again and found it insufficient -- Rich's column-width-based ellipsis crops by raw display width with no awareness of file extensions and no fixed character budget, not the specific 25-char/extension-preserving scheme actually wanted. Replaced it with a new `_truncate_filename(name, max_len=25)` helper in `viewmd/render.py`, called from `render_directory_listing` on the bare name (before the `--depth` indent is prepended) for both the `level == 0` `Text`-cell-with-link branches and the `level > 0` plain-escaped-text branches, for both directory and file rows; removed `max_width`/`overflow` from the `Name` column's `add_column` call entirely (kept `no_wrap=True` only so Rich never re-wraps the already-short result). Scheme: a name `<= 25` chars is shown as-is; otherwise the extension (the substring from the last `.` onward, including the dot) is preserved in full, the ellipsis `...` goes immediately before it, and the stem is cut to whatever's left of the 25-char budget (`25 - len(extension) - 3`). Decisions made and documented in the helper's own docstring: (1) a subdirectory's trailing `/` is treated the same way a file's extension is -- always preserved, ellipsis before it -- so a long subdirectory name doesn't lose its trailing slash and stays visually identifiable as a directory; (2) a name with no `.` at all, or a dotfile like `.gitignore` where the leading dot has no stem before it, falls back to treating the whole name as the stem with the ellipsis at the very end and no extension to protect; (3) a degenerate case where the extension itself is long enough to leave no room for a stem-plus-ellipsis (budget `< 1`) abandons the scheme entirely and hard-truncates the raw string to `max_len`, as a floor rather than producing a negative-length slice. The link href for `level == 0` rows still uses the untruncated `name` -- only the displayed text is shortened, navigation is unaffected. Added seven focused unit tests for `_truncate_filename` directly (`tests/test_render.py`): untouched-when-short, truncated-to-25-with-extension-intact, the exact-25 and one-over-25 boundary, no-extension, a `.gitignore`-style dotfile, a long directory name's trailing slash preserved, and the degenerate-budget hard-truncation floor. Updated the two existing `render_directory_listing` long-name tests from the prior `max_width=55` fix to assert the new scheme instead (`test_render_directory_listing_caps_a_long_name_at_25_chars_preserving_extension`, replacing the old `..._with_an_ellipsis` test; the companion `Title`-column-widens test needed no behavioral change, only its comment). Verified visually against this project's own `issues/` directory (genuinely long real filenames): `render_directory_listing("issues", width=120, color=True)` and a real `tmux` (120x30) run of `./viewmd.sh issues/` both show e.g. `VIEWMD-0031-er-layout-legibility-review.md` (43 chars) rendered as `VIEWMD-0031-er-layo....md` (exactly 25 chars, `.md` intact, `...` right before it), `VIEWMD-0028-flowchart-subgraph-membership-by-edge.md` as `VIEWMD-0028-flowcha....md`, and the short `archive/` subdirectory row left untouched since it's well under 25 chars -- table borders intact, no `›` truncation marker, scrollbar rendering correctly. Ran `./run-tests.sh`: 1175 tests pass (7 new), ruff and pip-audit clean; `issues --check` still fails only on the same pre-existing, unrelated `effort:` gap on VIEWMD-0090/VIEWMD-0091 noted in the prior reviews, unrelated to this change.
- (maintainer feedback, quick fix): right-aligned the `Size` column (`table.add_column("Size", no_wrap=True, justify="right")` in `render_directory_listing`), matching conventional numeric/size-column alignment (`ls -l`, `du`). Verified visually against `issues/` that byte sizes and `"N items"` entry counts now align on their right edge; no existing test asserted the column's prior left-alignment/whitespace positions, so nothing needed updating beyond a new regression test (`test_render_directory_listing_size_column_is_right_aligned`) asserting the rendered cell's trailing edge matches the column border. `./run-tests.sh`: 1176 tests pass (1 new), ruff and pip-audit clean; `issues --check` unaffected (same pre-existing, unrelated `effort:` gap).
- (agent, width-reset-on-navigation fix per maintainer's manual testing feedback): The maintainer ran `viewmd issues/` interactively (no `--width` flag, a wide pane) and found that clicking into a `.md` file from within the directory listing rendered it at the full terminal width instead of the document's normal prose-capped default -- opening the same file directly (`viewmd file.md`) respected the cap correctly, so the bug was specific to navigating *into* a document from a listing. Root cause: `viewmd/__main__.py` already computes two different widths for the single-directory-path case -- `width` (a document's own default, prose-capped when `--width`/config is unset) and `directory_width` (VIEWMD-0071's default-to-full-terminal-width value for the listing table itself) -- but only `directory_width` was ever threaded through to `viewmd/pager.py`'s `display_directory_listing` and on into `viewmd/interactive_pager.py`'s `run_directory_listing`/`_run`. Inside `_run`, `configured_width` was set once at the top of the session from whichever single width it was given and never updated again, including on a click-navigate -- so a `.md` file opened from inside a directory-listing session inherited the *listing's* full-width baseline rather than getting its own default. This was invisible whenever `--width` was passed explicitly, since `width == directory_width` in that case, matching how the maintainer's earlier testing in this issue never caught it. Fix: extended `open_path`'s return contract (both `run()`'s own doc-to-doc `open_path` and `run_directory_listing()`'s dir-or-file `open_path`) from a 3-tuple to `(loader, display_name, doc_dir, default_width)` -- `run()`'s is a no-op change (always its own `width`); `run_directory_listing()`'s `open_path` now returns `directory_width` for a subdirectory target and a newly-added `width` parameter (the document default, following the same two-widths-at-once pattern `run_multi_file` already uses for its own `width`/`directory_width` pair) for a `.md` file target -- threaded through from `viewmd/__main__.py` (now passing both `width` and `directory_width` to `display_directory_listing`) through `viewmd/pager.py` to `run_directory_listing`, whose own `width` parameter was renamed to `directory_width` to match that existing naming convention. `_run`'s click-dispatch block now unpacks the 4-tuple and reassigns `configured_width`/`width_is_full` from the newly-opened target's own `default_width`, rather than treating `configured_width` as fixed for the whole session; the `nav_stack` entry pushed on every click-navigate (used by the `B` back key) now also saves/restores `configured_width` alongside the `top`/`left_col`/`full_width_active` it already saved, for the identical reason those are saved per-frame rather than re-read at pop time -- going back to a previously-visited target has to restore *that* target's own width, not whatever width happened to be active when `B` was pressed. VIEWMD-0090-overlap risk flagged, per AGENTS.md's own guidance on this exact class of semantic conflict (the VIEWMD-0080/VIEWMD-0093 `loader(width)` shape collision): VIEWMD-0090 is a separate, not-yet-merged branch that independently turns this same single `nav_stack`/`B` mechanism into a `nav_stack`/`fwd_stack` pair (`b`/`f` keys) and changes what gets pushed onto them -- both branches touch this stack entry's tuple shape, so a `--no-ff` merge of both into `develop` is likely to auto-merge cleanly without a textual conflict while still being semantically wrong (e.g. losing the `configured_width` slot added here). Left an explicit comment at the `nav_stack` declaration in `viewmd/interactive_pager.py` for whoever does that merge, and did not attempt to merge in or coordinate with VIEWMD-0090's own changes, per the maintainer's instruction. Added tests in `tests/test_interactive_pager.py`: three new end-to-end tests drive the real event loop via a pipe standing in for `/dev/tty` (the same technique the existing VIEWMD-0094 tests use for `run()`), simulating an actual SGR mouse click on a `.md` file row -- `test_navigating_into_a_file_from_a_directory_listing_uses_the_files_own_default_width` (the exact bug scenario: `width`/`directory_width` differ, asserts the opened document is loaded at `width`, not `directory_width`), `test_going_back_from_a_navigated_file_restores_the_listings_own_width` (click in, `B` back out, asserts every re-render of the listing after that is at `directory_width`), and `test_navigating_into_a_file_with_explicit_width_is_unchanged` (both widths equal, confirming no visible behavior change so a future regression here would be caught). Also updated the existing lower-level `open_path`-contract tests (`test_run_directory_listing_wires_doc_dir_and_subdirectory_open_path`, `test_run_directory_listing_open_path_opens_an_md_file`, and the depth-carries-over/scrollbar/fallback tests) for the new 4-tuple shape, giving `width`/`directory_width` deliberately different values in the two that assert on `default_width` directly to prove the right one comes back for each target type. Updated the `fake_run`-style test doubles in `tests/test_main.py` and `tests/test_pager.py` that call `display_directory_listing`/`run_directory_listing` to accept and pass through both widths. `./run-tests.sh`: 1179 tests pass (6 new), ruff and pip-audit clean; `issues --check` still fails only on the same pre-existing, unrelated `effort:` gap on VIEWMD-0090/VIEWMD-0091 noted in every prior review in this section. Verified end-to-end in a real terminal (not just tests, per AGENTS.md's VIEWMD-0043 lesson): used `tmux` (220x50, no `--width` flag) to run `./viewmd.sh issues/` against this project's own `issues/` directory -- the initial listing renders its table at the full 220-column pane width as before; clicking the `README.md` row (a synthesized SGR mouse click) opens it wrapped at ~100-104 columns (the prose-readability cap), not 220; pressing `B` returns to the directory listing, which is back to its own full-220-column table layout, confirming both the fix and the back-navigation restore work correctly against a real terminal, not just the simulated-pipe tests.
