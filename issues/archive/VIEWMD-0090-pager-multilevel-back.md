---
id: VIEWMD-0090
title: Multi-level navigation history in the interactive pager (beyond one-step B)
status: implemented
area: [pager]
effort: medium
created: 2026-08-19
updated: 2026-08-20
accepted_by: George Moses <gmo6nl@gmail.com>
accepted_at: 2026-08-20
commits: [bbedb57, 4c8472e, f093fd4]
related: []
supersedes: []
changelog: "[1.47.0]"
reason:
---

# Multi-level navigation history in the interactive pager (beyond one-step B)

## Summary

Extend the interactive pager's link-following navigation (clicking a wikilink/Markdown link that resolves to a local `.md` file, per `viewmd/interactive_pager.py`) so `B` walks back through a full history stack rather than a single previous file, and add a forward complement so a back step can be redone.

## Motivation / problem

README documents `B` as "go back to the file you navigated from" — following the README's own wording, this reads as one step, not a stack. Clicking through several linked notes in a vault (note A → B → C) and wanting to return all the way to A means pressing `B` repeatedly while mentally tracking how many hops deep the session is; there's no visible confirmation of where each `B` press lands relative to the whole trail, and no way to go forward again after backing up too far.

## Requirements

1. MUST track a full history stack of visited documents (not just the immediately-previous one) for the lifetime of one pager session.
2. MUST make `B` pop one entry off that stack per press, correctly returning through multiple hops (A→B→C, then `B`,`B` returns to A).
3. MUST add a forward key (e.g. `F`) that re-descends the stack after one or more `B` presses, inert (no-op, same as `B` is today when there's nothing to go back to) when there is nothing to go forward to.
4. MUST preserve each visited document's scroll position on both back and forward navigation (matches current single-step `B` behavior, which already restores position — confirm and keep this, don't regress it).
5. MUST NOT change behavior for the non-link-following case (plain scrolling, search, ToC popup) at all.
6. SHOULD show current position in the trail somewhere discoverable (e.g. in the `?` help screen's `B`/`F` description, or a status-line hint), since a multi-level stack is easy to lose track of without any feedback.

## Non-goals

- No persistence of history across separate `viewmd` invocations (in-session only).
- No change to the existing single-document-only restriction on link-following (multi-file/directory-listing views still have no "current file" to navigate from, per the README's existing footnote).

## Design notes / links

Extends the existing back-navigation plumbing already present in `viewmd/interactive_pager.py` (the `B` key and whatever single-slot state backs it today) into a stack rather than replacing the mechanism.

## Acceptance / verification

New tests in `tests/test_interactive_pager.py`: a three-hop link-following sequence, back-back returns to the origin, forward-forward re-reaches the final hop, scroll position restored at each step. `./run-tests.sh` green.

## Peer review

- **Claude Sonnet 5** (agent, implementer self-review -- not the independent pass the Definition of Done still requires before "commit and close this out?"), 2026-08-20: Implemented in `viewmd/interactive_pager.py`'s `_run()`. Notable finding before writing any code: the pre-existing `nav_stack` (VIEWMD-0076) was already a plain `list` popped via `.pop()`, so requirements 1/2 (a full multi-hop back stack, `B`,`B` walking A→B→C back to A) were *already* satisfied by the existing code -- the README's "go back to the file you navigated from" wording undersold what was actually there. The only real gaps were requirement 3 (no forward complement existed at all) and requirement 6 (no discoverable trail position). Added a mirror-image `fwd_stack`: `B` pops `nav_stack` and pushes the document being left onto `fwd_stack`; `F` is the exact reverse. A fresh link-navigation clears `fwd_stack` (browser-style forward-history invalidation) -- not explicitly required by the issue but a design decision I made to avoid `F` resurrecting a document the reader has since branched away from; covered by its own test. `_keybind_help`'s `has_back: bool` became `back_count`/`forward_count: int`, so the echo-area hint reads e.g. `B: back (2)` / `F: fwd (1)` -- satisfies requirement 6's SHOULD via a live count in the status line; the `?` help screen's own `B`/`F` rows describe the mechanism in words but don't carry a live count (static table, no per-frame state) -- a scope judgment call, flagged for the maintainer rather than decided unilaterally.
  Self-review found and fixed three real bugs before landing, not just a description of the diff: (1) initial acceptance test wrote all synthetic input in one `os.write` before starting `run()` -- with two navigating clicks queued at once, VIEWMD-0094's `_drain_paired_sgr_release` (unrelated pre-existing code, out of this issue's scope to touch) greedily drained the *second* click's own release bytes during the *first* click's drain call, leaving a stray release sequence that the next `_read_event` misparsed as a spurious `Event('key', '')` and an extra redraw -- fixed by feeding the test's input in timed chunks from a background thread instead (a test-harness fix, not a pager fix; documented in `_run_pager_with_input`'s own docstring). (2) The first version of the click-coordinate math for the acceptance test didn't add `_scrollbar_reserved()`'s column offset, so both scripted clicks silently missed the link and the test was passing for the wrong reason (never actually left document A) until frame-by-frame tracing caught it. (3) An off-by-one in the test's own frame-index arithmetic (miscounted the number of navigating clicks contributing to the running total). None of the three were bugs in the shipped `B`/`F` implementation itself -- all were in the new test's own harness/math -- but each would have made the acceptance test either fail confusingly or, worse, pass while testing the wrong thing.
  `./run-tests.sh`: pytest (1148 passed) and ruff (including security rules) both green. `issues --check` fails, but only on VIEWMD-0089/VIEWMD-0091 -- two unrelated issues already `status: in-progress` with a missing `effort:` field as of this worktree's own branch point (`2a5f298`, confirmed via `git log`), not touched by this diff and out of this issue's scope to fix.
- **Claude Sonnet 5** (agent, independent review), 2026-08-20: Reviewed `git diff develop...feature/VIEWMD-0090` fresh, without relying on the implementer's own self-review notes above beyond using them as a pointer to where to look. Traced every `nav_stack`/`fwd_stack` reference in `viewmd/interactive_pager.py` (`grep -n`) and confirmed there is exactly one navigation entry point in the whole file (`_run`'s single `elif ev.kind == "click"` link-follow branch, ~line 2000-2051), shared by both `run()`'s in-text file links and `run_directory_listing()`'s subdirectory rows via the same `open_path` abstraction -- so requirement 3/5's "fresh navigation clears `fwd_stack`" and "B/F symmetry" both hold structurally, not just in the one scenario the implementer's own acceptance test happens to exercise, since there is no second code path that could diverge from it. The B/F key handler (`ev.value in ("B", "F")`) is a single shared block using `src_stack`/`dst_stack` aliasing rather than two separately-written branches, so there's no risk of the kind of asymmetry (e.g. scroll restored on B but not F) that duplicated code invites -- confirmed by reading the block directly rather than trusting the docstring's claim of symmetry. Requirement 5 (no behavior change to plain scrolling/search/ToC popup): grepped for every `nav_stack`/`fwd_stack` reference and found only three sites total -- the two stack declarations, the echo-area hint's `len()` reads (read-only, additive), and the one push/pop/clear site above; no shared mutable state leaks into the scrollbar-click, ToC-anchor-click, search, or resize branches, which don't reference either stack at all. Requirement 6: agree with the implementer's own flagged judgment call that a live count in the echo-area hint (`B: back (2)` / `F: fwd (1)`) satisfies the SHOULD on its own -- it's the discoverable-without-opening-anything surface a reader actually glances at during scrolling, and the static `?` screen listing `B`/`F`'s mechanism in words is adequate there since a live count is architecturally awkward for a screen with no per-frame refresh; not a blocker. Read the new tests in `tests/test_interactive_pager.py` end to end: the three-hop trail test asserts against real distinguishing content per frame index (not just "some navigation happened"), covers exact scroll-position restoration in both directions (not just home-position), and both the no-op-forward and forward-stack-invalidation cases are exercised as separate tests with their own real on-disk fixture files -- these would catch a real regression in the B/F mechanism, not just exercise it. Checked the `chunks`/paced-delivery fix for the paired-SGR-release drain bug the implementer's notes describe: the fix (background thread feeding chunks with a 0.15s sleep between them) is a legitimate simulation of real keystroke timing rather than a workaround that happens to dodge the symptom -- `_drain_paired_sgr_release`'s greedy-read behavior is real and pre-existing (VIEWMD-0094, correctly left untouched, out of this issue's scope), and pacing the input so a second click's bytes genuinely aren't on the fd yet when the first click's drain runs matches how an actual terminal session would deliver them; sound, not a symptom-mask. Ran `./run-tests.sh` myself in this worktree: 1148 passed, ruff (including security rules) clean, pip-audit clean; `issues --check` fails only on the same pre-existing VIEWMD-0089/VIEWMD-0091 missing-`effort:` issue, unrelated to this diff, matching the implementer's own account. No new bugs found in the shipped `B`/`F` implementation itself. Ready to merge as-is from a correctness/coverage standpoint; requirement 6's help-screen scope call is a judgment call I concur with rather than a gap.
- **Claude Sonnet 5** (agent, key-binding change per maintainer review feedback), 2026-08-20: After the review above, the maintainer looked at the shipped feature and asked for the trail keys to be lowercase `b`/`f` instead of uppercase `B`/`F` -- uppercase-only navigation reads as an odd outlier among the pager's otherwise-lowercase-first keymap. This surfaced a real conflict: lowercase `b` was already bound to "page back up" (the standard `less`-style binding, `ev.value in ("b", "backspace", "-")`), so plain remapping would have made `b` do two different things depending on nothing observable. Asked the maintainer outright how to resolve it; their explicit decision was to drop lowercase `b` from page-back-up entirely, keeping only `-`/Backspace for that, and give lowercase `b` exclusively to trail-back (with `f`, previously unbound, for trail-forward -- no conflict there). Changed every `B`/`F` reference in `viewmd/interactive_pager.py` to `b`/`f`: the event-dispatch check (`ev.value in ("B", "F")` -> `("b", "f")` and its `== "B"` branches), the `_HELP_GROUPS` `?`-screen rows, the `_keybind_help` echo-area hint chips and docstring, and every comment/docstring mention of the trail keys. Narrowed the page-back-up dispatch from `("b", "backspace", "-")` to `("backspace", "-")` and updated its `?`-screen row from `"b / - / Backspace"` to `"- / Backspace"`. Updated `README.md`'s keybinding table and prose (the `space / b` row, the `B`/`F` trail-navigation paragraphs, the directory-listing paragraph, and the footnote) to match. Updated every `Event("key", "B")`/`Event("key", "F")` and lowercase-`b`-page-back-up reference in `tests/test_interactive_pager.py` to lowercase `b`/`f`, and added two new tests -- `test_page_back_up_still_works_via_dash_and_backspace` (both `-` and Backspace still page back up, verified by comparing rendered frame content before/after) and `test_lowercase_b_alone_no_longer_pages_back_up` (pressing `b` alone after a page-down leaves the viewport untouched and produces the "No previous file to go back to" trail message instead) -- since no pre-existing test exercised the page-back-up dispatch through the real event loop to adjust. `./run-tests.sh`: 1150 passed (2 more than before, the two new tests), ruff (including security rules) clean, pip-audit clean; `issues --check` still fails only on the same pre-existing VIEWMD-0089/VIEWMD-0091 missing-`effort:` issues, untouched by this change. Manually confirmed via the new tests that `b` alone no longer triggers page-back-up and that `-`/Backspace still do.
