# TODO

Implementation status lives in [issues/](issues/README.md), not here — each unit of work is one
Markdown file with a `status:` (proposed / in-progress / implemented / superseded / rejected),
indexed at [issues/README.md](issues/README.md#index).

This file is just a pointer for newcomers landing on the repo root; see
[AGENTS.md](AGENTS.md) for how the issue-first process works end to end.
